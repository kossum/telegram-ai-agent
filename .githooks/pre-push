#!/bin/sh
# Pre-push: central codex-sync guard (skipped if the script isn't on this box).
set -eu

repo=$(git rev-parse --show-toplevel)
guard="$HOME/.claude/scripts/codex-sync-git-guard.py"
if [ -f "$guard" ]; then
  exec python3 "$guard" pre-push --scope project --repo "$repo"
fi
